# Bob the Conductor — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# ─── Server ─────────────────────────────────────────────────────────────────

# Public URL where this server is reachable (used for Twilio signature validation
# and Twilio Media Stream WebSocket URL). Must be HTTPS in production.
SERVER_URL=https://bob.symphonysmarthomes.com

# Internal port the Node.js server listens on
PORT=3000

# Log level: error | warn | info | debug
LOG_LEVEL=info

# Node environment: development | production
NODE_ENV=production

# ─── Twilio ──────────────────────────────────────────────────────────────────

# Found at https://console.twilio.com → Account Info
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# The Twilio phone number Bob answers (E.164 format)
TWILIO_PHONE_NUMBER=+13035550100

# ─── OpenAI ──────────────────────────────────────────────────────────────────

# Your OpenAI API key — must have access to the Realtime API (gpt-4o-realtime)
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ─── Google Calendar ─────────────────────────────────────────────────────────

# Service-account JSON key path (relative to project root)
GOOGLE_SERVICE_ACCOUNT_KEY=./config/google-service-account.json

# Calendar ID for scheduling service calls
GOOGLE_CALENDAR_ID=symphony@symphonysmarthomes.com

# ─── Daily Summary ───────────────────────────────────────────────────────────

# Recipient email for the nightly call-log summary
SUMMARY_EMAIL=mearley@symphonysmarthomes.com

# SendGrid API key (used by daily_summary.js)
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ─── Database ────────────────────────────────────────────────────────────────

# Path to the SQLite database file (auto-created on first run)
DB_PATH=./data/bob.db
