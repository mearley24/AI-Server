# Copy to .env at repo root or inject via Docker — see AGENTS.md
REDIS_URL=redis://redis:6379
# ACTIVE_CLIENT emails: one Zoho draft per tick (requires OPENAI_API_KEY, Zoho creds)
# AUTO_RESPONDER_ENABLED=true
