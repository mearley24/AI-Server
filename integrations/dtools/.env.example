# =============================================================================
# D-Tools Cloud Bridge — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env && nano .env
# =============================================================================

# REQUIRED: Your D-Tools Cloud API key
# Generate at: D-Tools Cloud → Settings → Integration → Developer → API Keys
DTOOLS_API_KEY=your-api-key-here

# Bridge server port (default 5050)
DTOOLS_BRIDGE_PORT=5050

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
