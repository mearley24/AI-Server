# Symphony Smart Homes — Home Assistant Integration
# Environment Variables Template
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   nano .env
#
# The .env file is automatically loaded by ha_bridge.py, ha_mqtt_agent.py,
# and the Docker Compose stack.

# ---------------------------------------------------------------------------
# Home Assistant
# ---------------------------------------------------------------------------

# IP address or hostname of your Raspberry Pi running Home Assistant
# Use the Pi's static IP for reliability (e.g. 192.168.1.100)
HA_URL=http://PI_IP:8123

# Long-Lived Access Token from Home Assistant
# Generate at: HA Profile → Long-lived access tokens → Create Token
# Name it "Bob the Conductor" for easy identification
HA_TOKEN=your-long-lived-access-token-here

# WebSocket URL for real-time event stream
# Usually same host as HA_URL with ws:// protocol
HA_WEBSOCKET_URL=ws://PI_IP:8123/api/websocket

# ---------------------------------------------------------------------------
# MQTT Broker (Mosquitto on the Pi)
# ---------------------------------------------------------------------------

# Same IP as the Pi (Mosquitto runs on the same host as HA)
MQTT_BROKER=PI_IP

# Default Mosquitto port (change if you use TLS: 8883)
MQTT_PORT=1883

# MQTT credentials (set in Mosquitto's password file on the Pi)
# Default HA Mosquitto add-on user is "homeassistant"
MQTT_USER=homeassistant
MQTT_PASSWORD=your-mqtt-password-here

# ---------------------------------------------------------------------------
# Camera Storage
# ---------------------------------------------------------------------------

# Where to save motion-triggered and on-demand camera snapshots
# Use an absolute path on Bob's filesystem for production
CAMERA_SNAPSHOT_DIR=./snapshots

# ---------------------------------------------------------------------------
# Optional: Service Configuration
# ---------------------------------------------------------------------------

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# OpenClaw network name (for Docker)
OPENCLAW_NETWORK=openclaw-net

# Bob's identifier on the network
BOB_CLIENT_ID=bob-conductor
