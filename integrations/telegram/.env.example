# Bob the Conductor — Telegram Bot Environment Variables
# Copy this file to .env and fill in your actual values.
# NEVER commit .env to git.

# ─────────────────────────────────────────────
# REQUIRED — Bot won't start without these
# ─────────────────────────────────────────────

# Your bot token from @BotFather
# See create_telegram_bot.md for setup instructions
TELEGRAM_BOT_TOKEN=your-bot-token-here

# Your personal Telegram chat ID (numeric)
# Get it from @userinfobot or the /getUpdates API endpoint
TELEGRAM_OWNER_CHAT_ID=your-numeric-chat-id

# ─────────────────────────────────────────────
# OpenClaw (required for most commands)
# ─────────────────────────────────────────────

# Base URL for the OpenClaw API
# On the same Docker network: http://openclaw:3000
# If running bot natively on Mac Mini: http://localhost:3000
OPENCLAW_API_URL=http://openclaw:3000

# ─────────────────────────────────────────────
# Bob's internal API (calls, calendar, etc.)
# ─────────────────────────────────────────────

BOB_API_URL=http://localhost:8080

# ─────────────────────────────────────────────
# Home Assistant (cameras, device states)
# ─────────────────────────────────────────────

HOME_ASSISTANT_URL=http://homeassistant:8123

# Long-lived access token from HA:
# Profile → Long-Lived Access Tokens → Create Token
HOME_ASSISTANT_TOKEN=your-home-assistant-long-lived-token

# ─────────────────────────────────────────────
# Local Data Paths
# ─────────────────────────────────────────────

# SQLite DB for ClawWork earnings (fallback if API is down)
CLAWWORK_DB_PATH=/data/clawwork/earnings.db

# SQLite DB for voice receptionist call log (fallback)
VOICE_DB_PATH=/data/voice/calls.db

# ─────────────────────────────────────────────
# Optional
# ─────────────────────────────────────────────

# Log verbosity: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Timezone for quiet hours and digest scheduling
TZ=America/Denver
