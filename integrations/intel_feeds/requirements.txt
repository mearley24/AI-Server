# Intel Feeds — Python dependencies
# No API keys required — all public endpoints

# Async HTTP client
httpx>=0.27.0

# Redis async client
redis>=5.0.0

# RSS/Atom feed parsing
feedparser>=6.0.11

# Standard library coverage (already available in Python 3.12):
#   asyncio, sqlite3, hashlib, json, logging, re, time, pathlib
#   http.server, threading, signal, argparse, collections
