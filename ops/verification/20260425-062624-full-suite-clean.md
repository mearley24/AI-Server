# Full Suite Clean — 2026-04-25T06:26:24Z

## Result

573 passed · 0 failed · 0 errors · 4 warnings (FastAPI on_event deprecation, pre-existing)

## Fixes applied

### 1. test_profiles_schema (test was stale)
- **File:** `ops/tests/test_client_intel_classifier.py:183`
- **Root cause:** assertion checked for `category` and `work_confidence` — column names from the threads table. The profiles table schema uses `relationship_type` and `confidence` (correct modern names, matching `scripts/client_intel_backfill.py:159-178`).
- **Fix:** Updated assertion to `{"profile_id", "contact_handle", "relationship_type", "confidence"}`.

### 2. test_plist_has_label[com.symphony.self-improvement.plist] (plist was malformed XML)
- **File:** `setup/launchd/com.symphony.self-improvement.plist:10`
- **Root cause:** XML comment body contained `--dry-run` — the `--` double-dash sequence is forbidden inside XML comments per the XML 1.0 spec. Python's `xml.etree.ElementTree` raised `ParseError: not well-formed (invalid token): line 10, column 52`.
- **Fix:** Rephrased comment to remove the double-dash: replaced `` `bash ... --dry-run` `` with plain prose `(pass -dry-run flag)`.

### 3. test_git_helper_returns_on_timeout (undeclared fixture)
- **File:** `ops/tests/test_task_runner_git_timeouts.py:82`
- **Root cause:** Function signature declared `monkey_args: list` as a parameter. pytest treats any function parameter as a fixture name and looked for a `monkey_args` fixture — which doesn't exist. The parameter was never used inside the function body (the local `captured: list[dict] = []` was used instead). The `__main__` block called it with `([])`, showing it was a leftover artifact.
- **Fix:** Removed the `monkey_args: list` parameter from the function signature and updated the `__main__` call-site to match.

## Command run

```
python3 -m pytest ops/tests -q
```
