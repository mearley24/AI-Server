==============================================================================
RELIABILITY AND OBSERVABILITY AUDIT — 2026-04-18 11:35 MDT
==============================================================================
Generated: 2026-04-18T11:35-06:00
Operator:  Cline (autorun, standing approval for low-risk repo-safe work)
Delta against: 20260417-093900-reliability-observability-audit.txt and
               20260417-094200-reliability-observability-final.txt
Baseline tools live in ops/verification/20260417-*-reliability-*.txt

This is a scoped re-audit of the reliability and observability category after
24 hours of production activity. The previous (April 17) audit left four
explicit follow-ups; this pass records their current status, discovers a
live production outage signal, and lands three low-risk hardening items
already committed in the same campaign.

------------------------------------------------------------------------------
1. Scope reviewed (what this audit looks at)
------------------------------------------------------------------------------
- ops/verification/ — retention, index, count, recency
- ops/AGENT_VERIFICATION_PROTOCOL.md — authoritative rule doc
- ops/task_runner_preflight.py — whitelist auto-heal + no-op suppressor
- ops/task_runner_health.py — runner health check (added disk.pressure here)
- ops/task_audit.py + ops/task_audit_index.py — artifact forensics
- ops/task_queue_status.py — queue visibility (added in autonomy gap-closer)
- ops/launchd_inventory.py — NEW this pass, plist missing-target sweeper
- scripts/pull.sh — git conflict-safe pull
- scripts/smoke-test.sh — end-to-end smoke
- scripts/verify-deploy.sh — manifest + service health matrix
- scripts/verify-cursor.sh — post-edit file existence check
- scripts/bob-watchdog.sh — self-healing loop (system-level)
- scripts/verification-index.sh / verification-prune.sh — artifact hygiene
- .gitattributes merge=ours whitelist
- .gitignore runtime-state exclusions
- Redis events:log capped 1000
- ~/Library/LaunchAgents/com.symphony.*.plist (45 plists)
- docker-compose healthchecks per service
- Cortex dashboard service tile
- STATUS_REPORT.md + its new auto-summarizer
- Lessons / Guardrails registries (ops/LESSONS_REGISTRY.md, ops/GUARDRAILS.md)
- Actual live state of Docker, Redis, disk, task runner

------------------------------------------------------------------------------
2. Items found and status
------------------------------------------------------------------------------
Key:  active = live and working
      partial = present but gap
      stale = obsolete or misleading
      inactive = file present but not running / not used
      unknown = exists, not verified this pass

A.  ops/verification/                             STATUS: active
    - 76 artifacts on disk, 652 KiB total, auto-grows ~6/day (healthy).
    - INDEX.txt present and regenerates on demand (104 lines, newest-per-topic
      + last 30 artifacts).
    - Prune script (verification-prune.sh) dry-runs clean: "nothing older
      than 90 days"; nothing to prune yet (repo is young).

B.  ops/AGENT_VERIFICATION_PROTOCOL.md             STATUS: active
    - 343 lines. Expanded since April 17 with approval tokens, dry-run lane,
    - launchd/tooling index, STATUS_REPORT conventions, learning loop.

C.  scripts/pull.sh                               STATUS: active
    - 108 lines, zsh, `set -euo pipefail`. No changes this pass.
    - Preflight whitelist auto-healed ops/verification/20260418-111824-
      preflight.txt on campaign start — 2 state files resolved, 0 unsafe.

D.  scripts/smoke-test.sh                         STATUS: active
    - Not re-run this pass (Docker is down — it would FAIL for the right
      reason). Script itself is sound.

E.  scripts/verify-deploy.sh                      STATUS: active
    - Current live run produces the expected matrix: Redis unreachable,
      openclaw/email-monitor/notification-hub/proposals/calendar-agent all
      FAIL (Docker daemon unresponsive). cortex FAILs-HTTP but actually
      replies (false negative due to curl racing). Note the tool is
      *correctly* alarming on the real outage.

F.  scripts/verify-cursor.sh                      STATUS: active (unchanged)

G.  scripts/bob-watchdog.sh                       STATUS: partial (stale coverage claim)
    - 199 lines; runs via launchd. Recent log: `[ALERT] Docker Desktop
      is down` at 10:50:55 MDT — watchdog detected the outage.
    - PREVIOUSLY CLAIMED in 20260417 audit: "self-healing loop:
      Tailscale, DNS, Docker, Redis auth, openclaw, disk pressure."
    - ACTUAL: grep for disk|Capacity|pressure|df returns ZERO hits.
      The watchdog does NOT monitor disk at all. Previous audit
      misread the file. Flagging as a reliability gap.

H.  ops/task_runner_preflight.py                  STATUS: active
    - No-op suppressor resume-fix landed 2026-04-18 08:26 (6c6ef9e).
    - Confirmed: preflight ran once at 11:18:24 with 2 whitelisted
      resolutions and did NOT loop.

I.  ops/task_runner_health.py                     STATUS: active + extended
    - Added disk.pressure check this pass (§6 in the module docstring).
      Threshold: --min-free-gib (default 5 GiB). Uses shutil.disk_usage
      on REPO_ROOT because that's the filesystem Docker and git share
      on Bob.
    - Live run result:
        launchd.loaded   !!   plist exists but launchctl did not list it
        heartbeat.fresh  !!   11285s old (threshold 900s)
        queue.not_stuck  !!   1 task stuck >11000s
        verification.recent OK newest=launchd-inventory age=63s
        preflight.seen       OK newest=preflight age=920s
        disk.pressure    !!   free=3.17 GiB / 228.3 GiB (used 98.6%)
    - The runner is stuck; the health check correctly reports it.

J.  ops/task_audit.py / task_audit_index.py        STATUS: active (unchanged)

K.  ops/task_queue_status.py                      STATUS: active
    - Landed during the autonomy gap-closer. Usable for queue visibility.
    - Not re-run this pass; policy-level coverage is fine.

L.  ops/launchd_inventory.py                      STATUS: active (NEW)
    - Added this pass. Enumerates ~/Library/LaunchAgents/com.symphony.*.plist,
      shows which are loaded, and flags any that reference a missing
      ProgramArguments target.
    - First run (baseline committed at ops/verification/20260418-113241-
      launchd-inventory.txt) reports: 45 plists, 24 missing-target (silent
      drift). See §5 below for the full list.

M.  Redis events:log (1000 capped)                STATUS: active but unreachable right now
    - Policy unchanged; current outage is upstream (Docker daemon down).

N.  .gitattributes / .gitignore whitelist         STATUS: active
    - No changes this pass. Preflight continues to auto-resolve
      knowledge/markup_exports/.session_tracking.json and
      data/cortex/digests/**.

O.  Docker healthchecks                           STATUS: partial (unchanged gap)
    - client-portal still has no /health route. Deferred to its category.

P.  launchd plist suite                           STATUS: sprawling + silent drift
    - 45 plists installed (up from 47 in April 17 — two plists were
      removed, no inventory doc to track which).
    - 24 plists reference a script that does NOT exist on disk.
      Every one of those plists either fails silently on each tick
      or was intentionally left behind when the script moved.
      CLAUDE.md lesson #23 is about exactly this.
    - Detailed list:
        com.symphony.core-ops-health-hourly         -> orchestrator/core_ops_alert_runner.py
        com.symphony.decision-hygiene-hourly        -> trading/decision_hygiene.py
        com.symphony.employee-beatrice-bot          -> telegram-bob-remote/start_employee_bot.sh
        com.symphony.employee-betty-bot             -> telegram-bob-remote/start_employee_bot.sh
        com.symphony.failure-replay-queue           -> trading/failure_replay_queue.py
        com.symphony.focus-ops-monitor              -> tools/focus_ops_monitor.py
        com.symphony.graph-drift-watcher-daily      -> trading/graph_drift_watcher.py
        com.symphony.incoming-tasks                 -> orchestrator/incoming_task_processor.py
        com.symphony.mobile-api                     -> api/mobile_api.py
        com.symphony.notes-watcher                  -> tools/notes_watcher.py
        com.symphony.overnight-learner              -> tools/overnight_learner.py
        com.symphony.polymarket-hourly              -> integrations/polymarket/polymarket_hourly_scan.py
        com.symphony.polymarket-scan                -> integrations/polymarket/polymarket_scan.py
        com.symphony.quality-gate-nightly           -> trading/quality_gate_nightly.py
        com.symphony.service-sre-loop               -> trading/service_sre_loop.py
        com.symphony.signal-action-hourly           -> trading/signal_action_runner.py
        com.symphony.subscription-audit             -> integrations/telegram/subscription_audit.py
        com.symphony.trading-pnl-attribution-daily  -> trading/pnl_attribution_report.py
        com.symphony.trading-provider-slo-monitor   -> trading/provider_slo_monitor.py
        com.symphony.trading-research-bot-hourly    -> trading/research_bot_hourly.py
        com.symphony.trading-research-daily-digest  -> trading/research_bot_daily_digest.py
        com.symphony.trading-research-quality-weekly -> trading/research_quality_loop.py
        com.symphony.trading-topic-graph-daily      -> trading/cross_topic_graph_runner.py
        com.symphony.worker-betty                   -> orchestrator/autonomous_worker.py

Q.  Cortex dashboard service tile                 STATUS: active (unchanged)

R.  STATUS_REPORT.md + auto-summarizer            STATUS: active
    - Summarizer landed at 10:45 MDT (b73d7f5). First digest in
      ops/verification/20260418-105302-status-report-summary.md.

S.  Self-healing mechanisms                       STATUS: partial
    - Layers present: bob-watchdog.sh (system), remediator container
      (Docker — currently OFFLINE), preflight (repo), x-intake listener
      watchdog (service).
    - Gaps:
        * bob-watchdog.sh does NOT monitor disk (corrected from April 17).
        * remediator container depends on Docker daemon → cannot self-heal
          a Docker outage (self-bootstrap problem).
        * No launchd schedule for verification-index.sh or
          verification-prune.sh despite April 17 final flagging them.

T.  Lessons / Guardrails registries               STATUS: active (new layer)
    - Learning miner + digest landed 2026-04-18. See
      ops/LESSONS_REGISTRY.md + ops/GUARDRAILS.md. Ready to absorb this
      audit's findings automatically on the next miner pass.

------------------------------------------------------------------------------
3. Pros (things that did their jobs today)
------------------------------------------------------------------------------
- bob-watchdog.sh caught the Docker Desktop outage at 10:50 MDT and wrote
  an ALERT to /usr/local/var/log/bob-watchdog.log. Without it, we'd be
  blind to the failure.
- x-intake host_redis_url fix (Cline, 09:04 MDT, 644e059) is verifiably
  landed — grep confirms the helper is in scripts/imessage-server.py.
  The bridge correctly stopped publishing to the Docker-only hostname;
  the current auth errors are now "Connection refused" (Docker down),
  not the old "Authentication required" (which would have masked the
  real fault).
- Preflight no-op suppressor (6c6ef9e) stopped the self-retriggering
  commit loop — heartbeat shows 0 spurious commits in the last tick.
- task_runner_health.py correctly flagged: launchd missing, heartbeat
  stale, queue stuck, verification still flowing. Five-of-six exact
  diagnoses from a tool that should be running every 2 min.
- verify-deploy.sh refreshed April 17 now surfaces the full outage
  matrix in one run (5 service FAILs + Redis FAIL). No stale probes.
- Preflight whitelist auto-heal still works: 2 files resolved
  silently on campaign start, 0 unsafe.

------------------------------------------------------------------------------
4. Cons / weaknesses / active incidents
------------------------------------------------------------------------------
CRITICAL (production is down right now)
- **Disk saturation**: /System/Volumes/Data at 98.6% (3.17 GiB free of
  228 GiB) as of 11:31 MDT. Single biggest contributor inside the repo
  is `data/audio_intake` at 14 GiB. Docker Desktop is unresponsive
  (daemon socket stalls >4s), which knocks out openclaw, redis,
  email-monitor, notification-hub, proposals, calendar-agent, x-intake,
  polymarket-bot. This is the upstream cause of every service FAIL
  observed in verify-deploy.sh and why x-intake's queue has had zero
  new rows in 24h despite 15+ iMessage X links.
- **Task runner is stuck**: heartbeat 11285s old (threshold 900s);
  launchctl does not list com.symphony.task-runner; one task has been
  pending since 17:05 MDT yesterday. Likely cause: the runner's
  `git push` began failing when the disk filled, which bricks the tick.

MEDIUM (documented reliability gaps)
- bob-watchdog.sh does NOT include a disk-pressure check. April 17
  audit claimed it did; it does not. Now partly mitigated by
  task_runner_health.py (disk.pressure added this pass), but that
  runs less often and can't auto-remediate.
- 24 launchd plists reference missing scripts (silent drift). Half are
  from the trading/ cleanup campaign. Every tick is a silent failure.
- No launchd schedule for verification-index.sh or verification-prune.sh.
  Artifacts accumulate; index is only regenerated on manual invocation.
- No automatic alert when task_runner_health exits 1. The plist runs
  every 2 min and writes a file but a FAIL state is only visible by
  reading the file. Cross-category with messaging.
- AGENTS_LAUNCHD_INDEX.md still does not exist as a human-authored map.
  The new ops/launchd_inventory.py produces a machine index but no
  prose explanation of WHY each job runs.

------------------------------------------------------------------------------
5. Better replacements or package suggestions
------------------------------------------------------------------------------
- macmon / istats — real-time hardware telemetry. Overkill; disk and
  Docker signals are sufficient.
- prometheus node_exporter + textfile collector — still overkill for
  one-host deployment.
- healthchecks.io external ping — useful as a dead-man for bob-watchdog
  and task-runner, but requires a secret and an external dependency.
  Skip today; revisit when we have off-site redundancy.

Net: stick with internal tooling. Current stack already covers 90%
of the surface. Fix the remaining 10% with small additions (which is
what this pass does).

------------------------------------------------------------------------------
6. Safe improvements made this pass (committed)
------------------------------------------------------------------------------
1. NEW  ops/launchd_inventory.py — enumerates all 45 Symphony plists,
   parses ProgramArguments, flags any whose target does not exist. Prints
   a human table and optionally persists to ops/verification/. Exit code
   1 when at least one plist has a missing target. (Low risk — read-only.)
   First baseline: ops/verification/20260418-113241-launchd-inventory.txt

2. MOD  ops/task_runner_health.py — added a sixth check, `disk.pressure`,
   with `--min-free-gib` CLI flag (default 5 GiB). Uses shutil.disk_usage
   on REPO_ROOT. Derived from today's observed outage: 3.17 GiB free on
   the volume Docker and git share caused Docker, push, and bridge all
   to fail together. Now the task-runner health plist will escalate to
   UNHEALTHY on disk pressure. (Low risk — additive, no existing check
   behavior changed.)

3. NEW  ops/verification/20260418-113500-reliability-audit.txt (this file)

4. NEW  ops/verification/20260418-113500-reliability-final.txt (companion)

5. OBSERVATION-ONLY: the x-intake subtask audit earlier in the same
   campaign (ops/verification/20260418-111941-x-intake-subtask-audit.txt)
   is kept as a data artifact. It documents: (a) the host_redis_url
   helper is confirmed present; (b) x-intake container is currently
   absent because Docker is down; (c) iMessage bridge now fails with
   "Connection refused" instead of "Authentication required," exactly
   the transition the April 18 09:04 fix predicted.

All changes respect the standing approval policy (low-risk, repo-safe
operational improvements inside AI-Server). No launchd plist was
installed or activated. No service was restarted. No secret was
touched. No existing file behavior was changed — only additive.

------------------------------------------------------------------------------
7. Remaining risks (documented, not remediated in this pass)
------------------------------------------------------------------------------
- **Disk saturation remediation.** Requires MEDIUM-risk work (either
  rotate/purge `data/audio_intake/` or offload to external storage).
  Separate blocker report required before action; not in the reliability
  category's write lane today.
- Task runner needs to be kicked after disk is freed
  (`launchctl kickstart -k gui/$(id -u)/com.symphony.task-runner`).
  Medium-risk; deferred.
- 24 launchd plists still reference missing scripts. Safe remediation
  is to either restore the scripts (if unintentional) or
  `launchctl bootout` and `rm` each plist (medium-risk; operator
  decision per-plist). A dedicated "launchd drift cleanup" pass
  should handle these one batch at a time. Documented by the new
  inventory tool so the next agent can act on it.
- verification-index.sh / verification-prune.sh still unscheduled.
- task_runner_health FAIL alerting still not wired to notification-hub
  (cross-category: messaging).
- AGENTS_LAUNCHD_INDEX.md (prose) still missing — the new inventory
  tool is machine-readable but not a narrative.

------------------------------------------------------------------------------
8. Next actions (ordered, low → medium risk)
------------------------------------------------------------------------------
LOW RISK (can be done by any agent without synchronous approval):
- Create ops/launchd/com.symphony.verification-index.plist (hourly) and
  ops/launchd/com.symphony.verification-prune.plist (weekly Sun 03:30)
  as repo artifacts. Install on a future pass.
- Add ops/launchd_inventory.py to a launchd schedule (daily) once the
  runner is healthy again.
- Extend bob-watchdog.sh to alert on <5 GiB free disk. Low-risk, but
  touches an already-active self-healer — better done in a dedicated
  watchdog-hardening pass with a cooldown added.

MEDIUM RISK (requires verification-report trail, not synchronous approval):
- Free up disk. Candidates in priority order:
    1. data/audio_intake/ — 14 GiB. Check if already ingested and
       whether source files can be pruned per a retention policy.
    2. Docker image prune (once daemon is back): `docker system prune`.
    3. ~/Library/Caches/ — ~126 MB, small.
    4. /tmp/imessage-bridge.log was 28k lines — prune to last 1k.
- Batch-clean the 24 drifted launchd plists: decide per-plist whether
  to restore the script or bootout the plist.
- Install the two new launchd schedules (index hourly, prune weekly).
- Kickstart the task runner after disk is freed.

HIGH RISK (explicit approval required):
- None in this category today. All remediation is repo-safe +
  system-hygiene.

------------------------------------------------------------------------------
9. April 17 follow-up status
------------------------------------------------------------------------------
From ops/verification/20260417-094200-reliability-observability-final.txt
→ "Next actions (deferred to later passes)":

  [ ] Schedule verification-index.sh via launchd (hourly).
      STATUS: still not scheduled. New plist file drafted for the next pass.
  [ ] Schedule verification-prune.sh --execute weekly (Sunday 03:30).
      STATUS: still not scheduled. Same plan.
  [ ] Wire task_runner_health FAIL → notification-hub iMessage.
      STATUS: cross-category (messaging). Unchanged.
  [ ] Write ops/AGENTS_LAUNCHD_INDEX.md + missing-target checker.
      STATUS: machine-readable sweeper SHIPPED this pass
              (ops/launchd_inventory.py + baseline report).
              Prose narrative still outstanding.
  [ ] Replace hardcoded port lists in smoke-test.sh / verify-deploy.sh
      with a single source of truth read from docker-compose.yml.
      STATUS: unchanged.

------------------------------------------------------------------------------
10. Handoff note
------------------------------------------------------------------------------
For the next reliability-category agent:

1. Start by reading this audit (20260418-113500-reliability-audit.txt)
   plus the companion final (20260418-113500-reliability-final.txt).
2. Read the latest launchd inventory
   (ops/verification/<latest>-launchd-inventory.txt) before touching any
   plist. Missing-target count MUST go down across passes, not up.
3. Run `python3 ops/task_runner_health.py --dry-run` as the first
   reality check — it now includes disk.pressure.
4. When disk is back above 5 GiB free and Docker is up, re-run
   `bash scripts/verify-deploy.sh` to confirm the service matrix
   is green.
5. Do NOT try to fix 24 plists in one pass. Batches of 4-6 at a time
   with a verification file each is the pattern that scales.

END OF AUDIT — see 20260418-113500-reliability-final.txt for the
compact summary of what was changed this pass.
